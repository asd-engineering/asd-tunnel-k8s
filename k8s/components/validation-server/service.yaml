apiVersion: v1
kind: Service
metadata:
  name: validation-server
  namespace: asd-tunnel-demo
  labels:
    app: validation-server
spec:
  type: ClusterIP
  selector:
    app: validation-server
  ports:
    - port: 8080
      targetPort: 8080
      name: http
