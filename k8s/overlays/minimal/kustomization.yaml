apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

components:
  - ../../components/validation-server

patches:
  - path: patch-single.yaml
    target:
      kind: StatefulSet
      name: asd-tunnel
