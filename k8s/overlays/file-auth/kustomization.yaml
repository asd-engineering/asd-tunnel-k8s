apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - pubkeys-configmap.yaml

components:
  - ../../components/validation-server

patches:
  - path: patch-file-auth.yaml
